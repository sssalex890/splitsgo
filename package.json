{
  "name": "splitgo",
  "version": "1.0.0",
  "description": "Split rental expenses fairly based on who actually used the gear",
  "main": "server.js",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "uuid": "^11.0.5"
  }
}
