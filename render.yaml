services:
  - type: web
    name: splitgo
    runtime: node
    buildCommand: rm -rf node_modules && npm install && npm rebuild better-sqlite3 --build-from-source
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
